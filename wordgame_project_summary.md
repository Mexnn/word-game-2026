# คู่มือโครงสร้างระบบและการพัฒนาโปรเจกต์ Word Master 2026 (ฉบับสรุปสถาปัตยกรรม)

เอกสารฉบับนี้จัดทำขึ้นเพื่อสรุปโครงสร้างการทำงาน ลอจิกของระบบ และรายละเอียดการอัปเดตล่าสุดของโปรเจกต์ **Word Master 2026** โดยเน้นอธิบายพฤติกรรมของระบบ (Behavior) และความเชื่อมโยงระหว่างหน้าบ้านและหลังบ้าน **(ไม่มีการแนบโค้ดดิ้งดิบ)** เพื่อให้ AI หรือนักพัฒนาท่านอื่นอ่านแล้วสามารถเข้าใจระบบเพื่อพัฒนาต่อได้ทันที

---

## 1. ภาพรวมของระบบ (System Overview)
* **รูปแบบแอปพลิเคชัน:** เว็บเกมทายคำศัพท์ภาษาไทย รองรับ 3 โหมดเวลา (1 นาที, 5 นาที, ไม่จำกัดเวลา)
* **สถาปัตยกรรม:** Serverless Architecture (Full-stack) รันบน Vercel
* **ฐานข้อมูล:**คลาวด์ฐานข้อมูลแบบ Real-time ด้วย Firebase Firestore
* **ธีมดีไซน์ล่าสุด:** Dark Edition (เน้นสีดำ #121212, แดงสด #ff4d4d, และเหลืองเตือน #ffeb3b) เพื่อสร้างอารมณ์ดุดัน ท้าทาย และกระตุ้นความตื่นเต้นของผู้เล่น

---

## 2. โครงสร้างไฟล์ในโปรเจกต์ (Project Structure)
* `vercel.json` -> ไฟล์คอนฟิกการทำ URL Rewrites เพื่อให้ Frontend เรียกเซิร์ฟเวอร์หลังบ้านได้ง่าย
* `package.json` -> ไฟล์จัดการ Dependencies และข้อมูลโปรเจกต์ของ Node.js (ใช้โมดูล `firebase-admin`)
* `index.html` -> โครงสร้างหน้าเว็บ แบ่งหลักๆ เป็น 3 ส่วน: หน้าตั้งชื่อ/เลือกโหมด, หน้ากระดานเล่นเกม, และหน้าตารางอันดับ
* `style.css` -> ไฟล์ควบคุมสไตล์ทั้งหมด ปรับปรุงเป็นโทน Dark Mode พร้อมแอนิเมชันปุ่มและไฮไลท์สีอันดับ 1-3 บนตารางคะแนน
* `script.js` -> ลอจิกหลักฝั่งผู้ใช้งาน (Frontend) ควบคุมการกดปุ่ม, ตัวจับเวลา, ลอจิกการส่งคำตอบ, และการดึงคะแนน Real-time
* `api/words.js` -> Serverless Function หลังบ้าน ทำหน้าที่เป็นคลังข้อความคำศัพท์และคำใบ้เพื่อความปลอดภัย
* `api/save-score.js` -> Serverless Function หลังบ้าน ทำหน้าที่ส่งคะแนนผ่าน REST API ไปบันทึกลง Firestore

---

## 3. ลอจิกการทำงานและการไหลของข้อมูล (Data Flow & Logic)

### 3.1 ระบบจัดการคลังคำศัพท์ (Backend Words API)
* **การทำงาน:** ย้ายข้อมูลคำศัพท์ทั้งหมดออกจากฝั่งหน้าบ้านมาไว้ที่หลังบ้านผ่าน `api/words.js` มีคำศัพท์เริ่มต้น 100 ข้อ แบ่งกลุ่มเป็น 10 หมวดหมู่หลัก (ธุรกิจ, เทคโนโลยี, สัตว์, อาหาร, สิ่งของ, ธรรมชาติ, อาชีพ, กีฬา, สุขภาพ, ยานพาหนะ)
* **Frontend Integration:** เมื่อผู้ใช้งานเปิดหน้าเว็บ `script.js` จะส่งคำขอแบบอะซิงโครนัส (`fetch`) ไปที่หลังบ้านทันที เพื่อนำคำศัพท์ทั้งหมดมาเก็บไว้ในอาเรย์ชั่วคราวบนเบราว์เซอร์เตรียมพร้อมสำหรับสุ่มเล่น

### 3.2 ระบบการเล่นเกมและควบคุมเวลา (Game Loop & Timer)
* **การเช็คความพร้อม:** มีระบบป้องกัน (Race Condition Protection) หากผู้เล่นกดเริ่มเกมขณะที่คำศัพท์ยังโหลดจาก API ไม่เสร็จ ระบบจะบล็อกการเข้าเกมและแจ้งเตือนให้รอ เพื่อป้องกันบัคหน้าจอเกมว่างเปล่า
* **การสุ่มโจทย์:** ระบบจะสุ่มหยิบดัชนี (Index) จากชุดคำศัพท์ที่ดึงมา ตัวสคริปต์จะทำการแปลงคำตอบเป็นตัวพิมพ์เล็ก (Lower case) อัตโนมัติเพื่อลดความผิดพลาดในการตรวจคำตอบ
* **ระบบนับถอยหลัง:** โหมด 1 นาที และ 5 นาที จะมีฟังก์ชัน `setInterval` ควบคุมการลดเวลาวินาทีต่อวินาที หากเวลาเหลือ 0 จะทำการตัดจบเกม ล้างข้อมูลชื่อในช่องพิมพ์ และเด้งกลับหน้าแรกอัตโนมัติ ส่วนโหมดไม่จำกัดเวลาจะซ่อนตัวเลขนับถอยหลังนี้ทิ้งไป

### 3.3 ระบบตารางคะแนนสูงสุด (Real-time Leaderboard)
* **ฝั่งหน้าบ้าน (ดึงข้อมูล):** ใช้ความสามารถของ Firebase `onSnapshot` เพื่อเกาะติดการเปลี่ยนแปลงของฐานข้อมูลแบบ Real-time โดยดึงข้อมูลจำกัดแค่ Top 10 ของแต่ละโหมด จัดเรียงคะแนนจากมากไปน้อย (`orderBy("score", "desc")`)
* **โครงสร้างการบันทึก (ฝั่งหลังบ้าน):** เมื่อผู้เล่นตอบถูก หน้าบ้านจะสั่ง `fetch` โพสต์ข้อมูลชื่อ คะแนน และโหมดส่งไปที่ `api/save-score.js` จากนั้นหลังบ้านจะแปลงข้อมูลโครงสร้างส่งต่อไปยัง Firestore endpoint โดยแยกบันทึกตามคอลเลกชัน ได้แก่:
  * `leaderboard_1m` (สำหรับโหมด 1 นาที)
  * `leaderboard_5m` (สำหรับโหมด 5 นาที)
  * `leaderboard_unlimited` (สำหรับโหมดไม่จำกัดเวลา)

---

## 4. รายการสิ่งที่ปรับปรุงล่าสุด (Changelog Summary)
1. **Decoupling Data:** แยกคำศัพท์ออกจากโค้ดฝั่งหน้าบ้านสำเร็จ ทำให้โค้ดสะอาดและรองรับการขยายคลังคำศัพท์ได้มหาศาลในอนาคต
2. **UI/UX Overhaul:** เปลี่ยนจากธีมสว่างธรรมดาเป็นธีม Dark Edition คุมโทนสีพื้นหลังเข้มและตัดขอบด้วยแสงสีแดง ปรับสีฟอนต์ข้อความแจ้งผลลัพธ์ (Feedback) ให้เรืองแสงชัดเจน (เขียวสด/แดงสด) เพื่อความสวยงามบนพื้นหลังมืด
3. **Smart Table Ranking:** ปรับปรุงลอจิกการเรนเดอร์ตารางอันดับ โดยใส่เงื่อนไขเฉพาะให้อันดับ 1, 2 และ 3 จะได้รับ Emoji เหรียญรางวัล (🥇, 🥈, 🥉) พร้อมเปลี่ยนสีแถวเป็นสีทอง เงิน ทองแดง ตามลำดับ ส่วนอันดับ 4-10 จะแสดงเป็นตัวเลขปกติ

---

## 5. คำแนะนำสำหรับ AI ในการทำงานต่อ (Prompt Guide for Next AI)
* **ต้องการเพิ่มคำศัพท์:** แจ้งให้ AI เข้าไปเพิ่มข้อมูลชุดวัตถุ `{ w: "คำศัพท์", h: "คำใบ้" }` ในไฟล์ `api/words.js` เท่านั้น หน้าบ้านจะอัปเดตสุ่มเจอเองโดยตรง
* **ต้องการทำระบบเลือกหมวดหมู่เล่น:** สามารถเพิ่มคีย์ประเภท เช่น `c: "หมวดหมู่"` ไว้ในไฟล์หลังบ้าน แล้วเขียนลอจิกคัดกรอง (Filter) เพิ่มเติมในฟังก์ชัน `nextQuestion()` บนหน้าบ้าน
* **ตรวจสอบสิทธิ์การเข้าถึงฐานข้อมูล:** เนื่องจากระบบหลังบ้านส่งข้อมูลผ่าน REST API ของ Firebase โดยตรง หากมีการเปลี่ยนชื่อโปรเจกต์ ต้องตรวจสอบตัวแปร `projectId` ในไฟล์ `api/save-score.js` ให้ตรงกันด้วย
